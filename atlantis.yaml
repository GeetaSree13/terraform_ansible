version: 3

projects:
  - dir: terraform
    workflow: project

workflows:
  project:
    plan:
      steps:
        - run: terraform init
        - run: terraform plan -out=tfplan
    apply:
      steps:
        - run: terraform apply tfplan
        - run: ../scripts/dynamic_inventory.sh
