- name: Configure logrotate for /log
  copy:
    dest: /etc/logrotate.d/logdir
    content: |
      /log/*.log {
        hourly
        missingok
        rotate 24
        compress
        notifempty
        create 0640 root root
      }